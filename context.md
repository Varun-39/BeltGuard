# PROJECT CONTEXT — Conveyor Belt Joint Rupture Detection System (SIH 26008)

> This file is the single source of truth for project state. Claude must read this file fully before starting work each session, and must update it after every meaningful change (new module built, decision made, dataset chosen, blocker hit). Never let this file go stale — treat it as the project's memory across sessions.

---

## 1. Problem Statement Reference

- **PS ID:** 26008
- **Title:** Belt Joint Rupture and Conveyor Belt Damages in Iron Ore Mining Industry — Intelligent Monitoring and Prediction
- **Organization:** Ministry of Steel
- **Department:** NMDC
- **Category:** Hardware
- **Theme:** Smart Automation
- **Core ask:** Build an intelligent, real-time, AI/IoT-based system to detect early-stage degradation of conveyor belt joints and belt body (cracks, tears, splice failure, misalignment, overheating) before rupture occurs, shifting maintenance from reactive to predictive.

## 2. Current Build Constraint (IMPORTANT)

- **No physical hardware is currently available** — no Jetson Nano in hand yet, no sensors, no camera, no belt rig.
- The project is being built **software-first, hardware-deferred**: every component must work end-to-end on simulated/public data now, and be swappable to real hardware later with minimal code change.
- Target edge device (once sourced): **NVIDIA Jetson Nano (4GB, B01)**.
- Do not assume hardware is present anywhere in code — always build an abstraction layer (e.g., a `DataSource` interface) so a simulated sensor feed and a real sensor feed are interchangeable.

## 3. System Scope (full vision, for reference — not all built yet)

1. IoT sensor layer: vibration, temperature, load/tension, belt tracking/misalignment, speed, acoustic
2. Vision layer: RGB camera (crack/tear/splice detection) + thermal camera (overheating detection)
3. Edge AI inference on Jetson Nano (TensorRT-optimized model)
4. Sensor fusion + rule-based/ML-based health scoring
5. Predictive analytics / Remaining Useful Life (RUL) estimation
6. Real-time dashboard with alerts (Normal / Warning / Critical)
7. Digital twin visualization
8. SCADA/PLC integration (simulated via Modbus for now)
9. Notification system (email/SMS escalation)

**Drone-based inspection is explicitly OUT of scope for this build.**

## 4. What We're Building RIGHT NOW (no-hardware phase)

- [x] **Vision defect-detection model** — YOLOv8n trained 120 epochs (57 min, RTX 5060). Test mAP50 **0.976** overall. `belt_defect.pt` (6.3 MB) + `belt_defect.onnx` (12.3 MB) in `vision/models/`. **Caveat: belt_joint validated on only 7 test instances — see blockers.**
- [x] **Simulated sensor data generator** — 5 sensors on one shared `BeltModel`; 7 physics self-checks passing
- [x] **MQTT pipeline** — amqtt broker + scenario publisher + subscriber round-trip verified end-to-end (`infra/test_mqtt_roundtrip.py`)
- [x] **Backend API + SQLite time-series store** — MQTT ingest, 2 Hz fusion/broadcast, REST + WebSocket; full-stack test passing
- [x] **Vision service** — ONNX inference, MJPEG stream, publishes detections to the same MQTT bus
- [x] **Sensor fusion / health scoring** — explainable rule engine, 7 end-to-end checks passing incl. correct causal attribution
- [x] **Predictive layer** — CWRU real-data validation of our vibration indicators (held-out-load split) + healthy-only anomaly detector + degradation-trend RUL with confidence bands
- [x] **Real-time dashboard** — Vite+React+Tailwind workstation: interactive 3D twin with component selection, contextual inspector (evidence, telemetry, camera, events, action), health history with RUL projection
- [x] **Digital twin** — react-three-fiber, troughed belt geometry driven by live telemetry (belt scrolls at measured speed, splice tracks real belt phase, idler colour = bearing health)
- [x] **SCADA/PLC integration** — hand-written Modbus/TCP server (12-register map), polls the backend at 1 Hz; 8 checks incl. a real pymodbus client reading it over TCP
- [x] **Notification / escalation** — `backend/notify.py`, stdlib SMTP, deadbanded state transitions carrying the fusion evidence; 8 checks. Unconfigured SMTP is a supported, clearly-declared mode
- [x] **Abstraction layer** — `DataSource` ABC + required `Reading.simulated` flag

## 5. Tech Stack Decisions Log

> Update this section every time a technology choice is finalized. Include the date and one-line reason.

| Layer | Choice | Reason | Status |
|---|---|---|---|
| Edge AI target | Jetson Nano 4GB | Chosen by team, hardware category PS | Confirmed, not yet in hand |
| Dev machine | Win 11, RTX 5060 Laptop 8GB, Py 3.13.9, Node 24 (no Docker/WSL) | Local GPU makes YOLO training feasible without cloud | Confirmed 2026-09-04 |
| Vision model | YOLOv8-nano (ultralytics) → ONNX export | ONNX is the exact handoff path to TensorRT on Nano; nano fits 4GB | Decided 2026-09-04 |
| Messaging | MQTT via **amqtt** (pure Python) | ~~Mosquitto in Docker~~ — Docker Desktop needs WSL2, not installed, costs admin + reboot. amqtt keeps `pip install -r requirements.txt` as the entire setup. Same protocol/topics/payloads. | **Verified working 2026-09-04** |
| Time-series store | SQLite (WAL mode) | Demo-scale data; InfluxDB adds an ops surface for zero demo benefit. Swap path documented in docs/ | Decided 2026-09-04 |
| Backend | FastAPI + WebSocket push | Async, fast, WS gives the dashboard live streaming without polling | Decided 2026-09-04 |
| Dashboard | **Vite** + React + Tailwind v4 + Recharts | ~~Next.js + shadcn~~ — this is one client-side live page; SSR/routing/RSC contribute nothing and shadcn would supply ~2 components we'd hand-roll anyway. Vite is lighter with faster HMR. | Built 2026-09-04 |
| Digital twin | react-three-fiber inside the dashboard app | One app, zero install for judges, shares the same live WS feed | Built 2026-09-04 |
| SCADA/PLC sim | pymodbus TCP server | Standard industrial handoff; documents the real-SCADA mapping | Decided 2026-09-04 |

## 6. Datasets Found / In Use

> Log every dataset considered, whether it was used, and why.

| Dataset | Purpose | Source | Used? | Notes |
|---|---|---|---|---|
| Conveyor-belt-damage (sample-wy2mp) | Vision — belt body defects | Roboflow Universe `sample-wy2mp/conveyor-belt-damage` | **YES — primary** | Verified via API 2026-09-04: **922 images, CC BY 4.0, 1 version, instance-segmentation.** Useful classes: Tear 689, patch work 405, impact damage 283, Puncture 120, Hole 42. Largest genuinely-useful belt defect set found. |
| Conveyor Belt Damage (test-yfiry) | Vision — **belt joint** | Roboflow Universe `test-yfiry/conveyor-belt-damage-ucjlj` | **YES — merged in** | Verified via API: **325 images, CC BY 4.0, 1 version.** Classes: Small Hole 232, Small Tear 212, Large Hole 205, Large Tear 188, **Belt Joint 41**. Only dataset found anywhere with an explicit *Belt Joint* class — indispensable given PS 26008 is about joints. 41 instances is thin; augmentation + a possible hand-labelled top-up is an open item. |
| Conveyor Belt Damage Detection (cctv-tarjun) | Vision | Roboflow Universe `cctv-tarjun/conveyor-belt-damage-detection-bvgsj-dk03r` | **NO — rejected** | Looked like the biggest win at 2,353 images, but API check showed it has **one class, `conveyor-belt`** (it localises the belt, it does not detect damage) and **`versions: 0`**, so nothing is downloadable. Image count alone would have been a misleading metric. |
| conveyor belt tear (samruddhi-uxs8x) | Vision — tear only | Roboflow Universe `samruddhi-uxs8x/conveyor-belt-tear` | Not yet | ~700 imgs, single-concept. Held in reserve as a top-up for the `tear` class if recall is weak after first training run. |
| NASA IMS Bearing (Univ. of Cincinnati) | Run-to-failure / RUL | data.gov `ims-bearings` | **NO — deliberately not used** | ~6 GB, and it is bearing run-to-failure. A conveyor splice fails by a different mechanism, on a different timescale, under different loading — an IMS-trained RUL model would be transferring across a gap wide enough to make its numbers dishonest. We use degradation-trend extrapolation instead and say so. Revisit if we ever get real belt run-to-failure history. |
| CWRU Bearing Fault | Vibration — **validates our indicators on real data** | `engineering.case.edu/sites/default/files/<n>.mat` (direct download, no auth) | **YES — in use** | 16 recordings: normal + inner_race + ball + outer_race, each at 0/1/2/3 hp, 12 kHz DE, 0.007in seeded defects. 1,537 windows. Used to prove RMS/kurtosis/crest-factor separate REAL faults, not just our simulator's. |
| _Thermal / acoustic belt data_ | Overheating, acoustic | — | **None found** | No open dataset for conveyor-specific thermal or acoustic faults located. These will be **synthetic**, with fault signatures derived from documented failure physics, and labelled SIMULATED in the UI. |

## 7. Decisions & Rationale Log

> Append-only log. Never delete old entries — strike through if superseded, but keep history visible.

- **2026-09-04 — Project gets its own git repo.** Found that `git rev-parse --show-toplevel` resolved to `C:/Users/Dell` — the entire home directory was an accidental uncommitted repo. Ran `git init` inside the project folder on branch `main` so project history is isolated. The stray home-dir repo was left untouched.
- **2026-09-04 — SQLite over InfluxDB for time-series.** A demo generates on the order of 10^5–10^6 rows; SQLite with a timestamp index handles that with zero ops overhead. InfluxDB would add a container, a query language, and a retention policy for no visible demo benefit. Swap path (same insert/query surface) documented rather than pre-built.
- **2026-09-04 — MQTT kept despite being "extra".** Unlike the DB choice, MQTT is not overhead: it is the actual boundary where a real Jetson replaces the simulator. Simulated publishers and real sensors publish to the same topics, so the hardware swap is a config change, which is the project's core non-negotiable.
- **2026-09-04 — Custom Next.js dashboard over Grafana.** Grafana is faster to build but is exactly what judges have already seen. Design quality is a scoring surface here, so the time cost is justified.
- **2026-09-04 — Always verify a dataset via the Roboflow API, never by its advertised image count.** The 2,353-image `cctv-tarjun` set looked like the best find by a wide margin; the API showed a single non-defect class and zero generated versions. Two of the four candidates were unusable for reasons invisible on the listing page.
- **2026-09-04 — Merge the two usable vision datasets as BOUNDING-BOX detection, not segmentation.** The larger set (922) is segmentation, the joint-bearing set (325) is boxes. Training seg alone would mean dropping the `Belt Joint` class, which is unacceptable for a problem statement about belt joints. Polygons convert down to boxes losslessly-enough for this purpose; boxes do not convert up. Cost accepted: we lose pixel-accurate damage area.
- **2026-09-04 — Unified 5-class taxonomy, with size dropped as a label.** `belt_joint`, `tear`, `hole`, `impact_damage`, `patch_repair`. The source sets' `Large Tear`/`Small Tear` distinction is collapsed because severity should be derived from measured box area, not from a labeller's subjective size call. `Human`, `Roller`, `Conveyor`, `Other Objects` are dropped as non-defects.
- **2026-09-04 — Synthetic sensor data is signature-driven, not noise-driven.** Where a real dataset exists (vibration), fault signatures are extracted from CWRU/IMS and used to shape the generator. Where none exists (thermal, acoustic), signatures come from documented failure physics. Random noise alone would not survive a judge's question.

- **2026-09-04 — Fusion is a rule engine, not a model.** Nobody stops a 2,000 t/h ore line on an unexplained 0.87. Every alarm emits the indicator, its measured value, and the standard behind the threshold (ISO 10816-3, bearing kurtosis rules, idler temperature practice). The planned ML anomaly/RUL layer sits *alongside* this as a catcher of unanticipated patterns, never replacing the explainable backbone.
- **2026-09-04 — Evidence combines by noisy-OR, `1 - prod(1-s)`.** Chosen over `max()` because corroboration should count (three indicators at 0.5 is worse than one at 0.5) and over `mean()` because averaging lets healthy channels dilute one genuinely alarming one.
- **2026-09-04 — CALIBRATION FIX: severity ramps top out at the physical worst case, not the alarm level.** First version anchored each ramp's upper bound near its alarm threshold, which pinned severity to 1.0 the instant a channel alarmed — the health score saturated at ~10/100 by the midpoint and the entire back half of the degradation timeline looked identical. Alarm placement is the job of the NORMAL/WARNING/CRITICAL bands. These bounds are the per-site tuning knob a real installation would adjust.

- **2026-09-04 — Heavy vision artifacts live outside the repo, at `%LOCALAPPDATA%\sih26\`.** The project sits under OneDrive, and OneDrive syncs by folder tree — it does not read `.gitignore`. A 141 MB dataset plus per-epoch checkpoints were being re-uploaded continuously, burning quota and bandwidth *and* throttling training. `%LOCALAPPDATA%` is never synced and is the conventional Windows home for regenerable data — and this data is regenerable, `prepare_dataset.py` re-downloads it. Resolved from the environment (not hardcoded to this user) via `vision/paths.py`, overridable with `SIH_DATA_DIR`. Final `.pt`/`.onnx` stay in the repo: small, and worth having backed up. Measured effect: 3.4x faster training.

- **2026-09-04 — Vision service runs ONNX, not the CUDA `.pt`, and it is measurably faster.** Benchmarked on 40 pre-loaded test frames: **ONNX 61.8 img/s vs CUDA .pt 30.4 img/s**. YOLOv8n at 640px is small enough that CUDA's per-call transfer/sync overhead outweighs its compute advantage on single frames. This also means the demo exercises the exact artifact that ships to the Nano. An auto-selecting dual-backend was built and then deleted once measured — it solved a problem that did not exist.
- **2026-09-04 — MEASURE BEFORE OPTIMISING (recorded because it nearly cost us).** An early benchmark showed ONNX at ~7 img/s and triggered an onnxruntime-gpu install that corrupted numpy. That benchmark did a `cv2.imread` per image: it was measuring **disk, not inference**. Real figure is 61.8 img/s.
- ~~**2026-09-04 — `_yolo_path` exists because of the apostrophe in `SIH'26`.**~~ **SUPERSEDED 2026-09-04 by the folder rename to `beltguard`; workaround deleted.** Original entry: ultralytics strips apostrophes from absolute `.pt` paths and then fails with FileNotFoundError on `SIH26\...`; `torch.load` and `check_file` handle the same path fine, so the bug is inside ultralytics' `.pt` loader. A cwd-relative path avoids it. **Latent hazard: expect the apostrophe to bite again in npm/Next.js tooling at the dashboard stage.**

- **2026-09-04 — CWRU evaluated by held-out OPERATING CONDITION, not a random window split.** A random split over windows cut from one continuous recording puts near-duplicate slices in train and test, which is the standard way CWRU results get inflated. We train on 0/1/2 hp and test only on the unseen 3 hp. Both numbers are printed. **They came out identical (100% / 100%, gap 0.0)** — the leakage concern was real but did not materialise here. The honest caveat is different: CWRU's seeded 0.007in defects are large and distinct (separable on kurtosis alone, normal ~2.9 vs outer_race ~7.8), so 100% reflects an easy benchmark, not a strong model. Real idler wear is gradual and messier.
- **2026-09-04 — RUL is degradation-trend extrapolation, and is labelled as such everywhere.** Not a learned RUL model — that needs run-to-failure history for *this* asset class, which nobody has for a conveyor splice. Trend extrapolation is what condition-monitoring practice actually uses when you have a health indicator and no failure history. It refuses to answer on a flat trend, reports a confidence interval rather than a point estimate, and fits only a recent window because degradation accelerates.
- **2026-09-04 — `DEMO_ACCELERATION` makes the compressed timeline explicit.** The scenario ramps a full healthy->rupture failure in 45 simulated minutes so a demo is watchable; a real splice takes 2-4 weeks. The constant (672x) is applied to the *fault ramp only* — belt revolutions, thermal lag and bearing defect frequencies still run at real rates — and RUL reports the real-world equivalent alongside sim time so nobody infers that belts fail in an afternoon.

- **2026-09-04 — Project renamed `SIH'26` -> `beltguard`; product name BeltGuard.** The apostrophe was not cosmetic: it broke ultralytics `.pt` loading outright, and npm/Next.js were the next likely victims. Renaming was the root-cause fix rather than accumulating per-tool workarounds. Backend also moved off port 8000 (owned by another local project) to **8010**.

- **2026-09-04 — Dashboard design taken from `ui-ux-pro-max`, with two deliberate departures.** Used its palette (#0F172A/#1B2336 dark tech + status green), Fira Sans/Fira Code pairing and dense-dashboard spacing. Departed on (a) **glassmorphism** — it was the recommended style, but frosted blur in a safety-critical control room trades legibility for decoration, so panels are solid with 1px borders; (b) **Next.js** — see stack table. Kept its accessibility requirement strictly: **status is never conveyed by colour alone** (every state carries a text label and a distinct glyph ● ▲ ■), and the streaming view has a Pause control.
- **2026-09-04 — VISION EVIDENCE NEEDS TEMPORAL PERSISTENCE (real bug found via the dashboard).** Scoring the instantaneous camera detection made the health index flap between 13 and 100 twice a second, because each frame's detection instantly drove `belt_body`. That is wrong for the real system too: a single frame from a camera on a vibrating conveyor must never drive a maintenance alarm. The backend now aggregates detections over a 6 s window, requires a class to appear in >=40% of recent frames before it counts, and uses the **median** area rather than the worst frame. Health swing over 20 samples dropped ~87 -> ~38, with the remainder being genuine degradation.
- **2026-09-04 — RUL must smooth before fitting.** Raw 2 Hz health carries real per-sample flicker; fitting a line straight through it produced r^2 0.04 and a nonsense 1874 points/hour trend. `_bin_median` bins to a median per time bucket first. Median not mean, so one frame catching a large tear cannot drag the whole trend.
- **2026-09-04 — Vision area thresholds are a COMMISSIONING PARAMETER, not a constant.** `_vision_evidence` bounds assume a wide-angle camera seeing full belt width, where a serious tear is a few percent of frame. The `--source testset` images are macro shots of damaged belts (area_frac up to 0.58), so they saturate every threshold and read belt_body as failed continuously — correctly for that framing. Mount height, lens and belt width all move these numbers.

- **2026-09-04 — Dashboard now answers "so what do I do?", not just "what is the state?".** Three additions: (a) the health trend projects the RUL trend forward as a dashed line with 95% bounds and a marked CRITICAL crossing, so "predictive" is a picture rather than a side-panel number; (b) a **Recommended Action** panel naming the repair, the location, the schedule window, and the planned-vs-unplanned downtime contrast — that contrast is the project's business case and nothing else on screen stated it; (c) an **Event Log** of confirmed state transitions. The event log is derived client-side from `/api/health/history`, which already existed — no new table, no new endpoint.
- **2026-09-04 — Alarm deadband on the event log.** Health hovering on the 80 threshold produced `NORMAL -> WARNING -> NORMAL` flapping. A new state must now hold for 4 consecutive samples before it is logged. An alarm list that flaps is the classic way operators learn to ignore one.
- **2026-09-04 — BUG: SQLite connection was shared across three threads with no lock.** `check_same_thread=False` permits cross-thread use but does NOT make a connection concurrency-safe; the MQTT callback thread, the asyncio broadcast task and FastAPI's handler threads interleaved statements and raised `sqlite3.InterfaceError: bad parameter or other API misuse` mid-query, surfacing as intermittent 500s on `/api/history`. The original comment claiming "writes are serialised by the connection's own lock" was simply wrong. Fixed with one `threading.Lock` around every statement; verified with 50 concurrent requests, all 200, zero errors.
- **2026-09-04 — BUG: three.js cannot parse CSS custom properties.** The digital twin passed `var(--color-ok)` tokens straight into materials, logging `THREE.Color: Unknown color model` every frame. `cssColor()` resolves tokens to literals for the WebGL side only, so `index.css` stays the single source of truth for the palette.
- **2026-09-04 — The embedded preview pane does not composite the WebGL canvas into screenshots.** The digital twin appears blank in captures while rendering correctly; `canvas.toDataURL()` (needs `preserveDrawingBuffer: true`) is the reliable way to verify it. Recorded because it cost real time chasing a rendering bug that did not exist — a blank twin in a screenshot is not evidence the twin is broken.

- **2026-09-09 — Modbus server is hand-written; pymodbus is kept only as the test client.** pymodbus 3.15 is mid-migration: `ModbusServerContext` is deprecated and rejected our writes (`ExcCode 6`), and the replacement `SimData` pre-builds its register block with no supported way to mutate values while the server runs — which is the entire job of a live bridge. FC3/FC4 over TCP is a 7-byte header plus a 5-byte request, so serving it directly was less code than working around that. Testing our server against pymodbus's *client* is a stronger interop claim than testing it against our own.
- **2026-09-09 — Register map design: HEARTBEAT and DATA_VALID are deliberately separate.** The heartbeat proves the bridge is alive; DATA_VALID proves the numbers are fresh. On losing the backend the bridge reports `ALARM_STATE=NO_DATA`, `DATA_VALID=0`, `RUL_DAYS=65535` and keeps the beat ticking, rather than leaving stale values a PLC would act on. `RUL_DAYS=65535` means unknown, kept distinct from `0` which would mean "failing now".
- **2026-09-09 — PERFORMANCE BUG: `localhost` costs ~2 s per request on this host.** Every backend endpoint measured a uniform ~2,050 ms, which saturated the SCADA bridge's 1 Hz poll loop. Cause: `localhost` resolves to `::1` first while uvicorn binds IPv4 only, so each request waits out an IPv6 connection failure before falling back. **`127.0.0.1` measures 15 ms — 136x faster.** All Python HTTP clients now use `127.0.0.1` explicitly. Worth knowing before blaming any future latency on the backend.
- **2026-09-09 — BUG: `asyncio.create_task()` without keeping a reference.** The SCADA poller was garbage-collected mid-run; the heartbeat froze while the server kept serving stale registers — the exact failure the heartbeat exists to expose. The task handle is now retained, and the poll loop catches all exceptions and degrades to NO_DATA rather than dying silently.

- **2026-09-09 — Alerts carry evidence, and have NO time-based rate limit.** The mail includes the top contributing indicators from the fusion layer, because "CRITICAL, health 32" is not actionable while "bearing housing 84 C" tells a fitter what to bring. The first version had a 5-minute rate limit on top of the deadband; it silently swallowed the "recovered" notice, leaving anyone who got the CRITICAL mail believing the belt was still down. The deadband plus "alert once per confirmed transition" already make spam impossible, so the timer was deleted rather than special-cased.
- **2026-09-09 — Unconfigured SMTP is a supported mode, not an error.** With no mail server the alert is logged and recorded with `delivered=False`, and `/api/alerts` reports `smtp_configured`. The escalation path is demonstrable in a demo without pretending mail was sent. Alerts generated from simulated sources say `[SIMULATED DATA]` in both subject and body, so an alert can never imply a real belt is failing.

- **2026-09-09 — Alarm deadband was implemented twice, in two languages.** The dashboard's event log re-derived state transitions from health history with its own `HOLD = 4`, while `notify.py` already computed confirmed transitions with `HOLD_SAMPLES = 4` to decide what to escalate. Two copies of one rule, guaranteed to drift. `notify.py` now logs *every* confirmed transition and flags which ones warrant a mail (`notified`); the dashboard reads `/api/alerts`. Net: less client code, one implementation, and the event log gained whether each transition actually reached a human.
- **2026-09-09 — Full dead-code review.** Removed: `seen_in` and `frames_considered` (computed into the vision payload, read by nobody), `hours_to_warning` and `WARNING_LEVEL` (computed and returned by RUL, consumed nowhere), and `Rul.method` in TypeScript (declared, rendered nowhere). Verified clean and kept: all FastAPI route handlers (referenced by decorator, not name), `.oxlintrc.json` (wired to `npm run lint`), `Detection` type, `/api/status` (ops endpoint, covered by `test_backend`).
- **2026-09-09 — `predictive/cwru.py` had no assertions.** It carried the project's strongest claim — that our indicators separate *real* faults — while being the only non-trivial module that could not fail. It now asserts kurtosis still separates outer-race faults, held-out-load accuracy stays above 0.90, false alarms stay under 15%, and the download produced enough windows.
- **2026-09-09 — Added `.env.example`.** `.env` is gitignored, so `ROBOFLOW_API_KEY`, `SIH_DATA_DIR` and the `SMTP_*`/`ALERT_*` variables were undiscoverable for anyone cloning the repo; they had been documented in three separate files and nowhere together.

- **2026-09-10 — Motion added, with a rule: animation must carry information.** Motion (motion.dev) for React, chosen over adding anime.js alongside it — two animation runtimes doing one job is bloat, not polish. All motion primitives live in `dashboard/src/components/motion.tsx` under one reduced-motion policy. What each animation *means*: animated numbers convey **direction and magnitude** of change (a health index snapping 76->61 reads as a glitch; one that visibly falls reads as deterioration); `AnimatePresence` on the evidence and event lists shows **what** changed, not just that the list differs; the gauge needle uses a spring because a real analog gauge has mass; the header status chip pulses on escalation so a state change registers even if the operator was looking elsewhere. Panel reveal is first-paint only — re-animating on every 2 Hz tick would make the dashboard twitch.
- **2026-09-10 — Three motion constraints enforced throughout.** (a) Exit is faster than entry (160 ms vs 340 ms) — arriving deserves a beat, leaving should get out of the way. (b) Transform and opacity only; subsystem bars and severity bars animate `scaleX`, never `width`, so nothing relayouts twice a second. (c) `prefers-reduced-motion` is a hard off, not a slowdown — and the CRITICAL alarm edge degrades to a *static* red border rather than vanishing, so the safety signal survives the setting.
- **2026-09-10 — CRITICAL alarm treatment sits on the viewport frame, not a panel.** A slow breathing red edge at `z-50`, readable from across a room at an angle by someone not looking directly at the screen — which is the actual condition of a control room.

- **2026-09-10 — Redesigned on Apple's Liquid Glass language.** Palette moved to Apple system colours (systemGreen/Orange/Red/Blue/Purple), type to Inter (closest free SF Pro analogue) + JetBrains Mono for telemetry, radii to 22/16/11 px. Glass is four stacked cues, not just blur: `saturate(180%) brightness(112%)` on the backdrop for vibrancy, a near-transparent vertical tint for body, `inset 0 1px rgba(255,255,255,0.55)` for the specular top edge, and an inset ring plus deep outer shadow for thickness. **The specular edge is what sells it** — blur alone reads as a frosted div.
- **2026-09-10 — The aurora field exists so the glass has something to refract.** Four slowly drifting colour wells behind everything. Over a flat dark fill `backdrop-filter` has nothing to bend and every glass panel collapses into a grey rectangle; this is the single most-missed prerequisite of glassmorphism.
- **2026-09-10 — Real chromatic refraction via SVG (`components/Glass.tsx`).** One `feTurbulence` field sampled by three `feDisplacementMap` passes at staggered scales (26/17/9), each isolated to one channel with `feColorMatrix`, recombined with `feBlend mode="screen"` — red bends most, blue least, as in a real lens. Fed to `backdrop-filter: url(#lg-refract)`. `colorInterpolationFilters="sRGB"` is mandatory and **must be camelCase in React**; the hyphenated SVG spelling is rejected as an invalid DOM prop and the filter silently runs in linearRGB.
- **2026-09-10 — BUG: a backdrop-filtered ANCESTOR breaks react-three-fiber's canvas sizing.** The digital twin went blank after the glass redesign: r3f measured its container as zero, left the canvas at its 300x150 default, and rendered nothing. Proven by toggling `backdrop-filter: none` at runtime, after which the canvas immediately sized to 809x474. Fix is a `.glass-solid` variant — identical radius, specular edge and shadow, no backdrop-filter — used via `<Panel solid>` for any panel hosting WebGL. This also matches the design rule already in force: **chrome is glass, content is opaque.**
- **2026-09-10 — BUG: `<Environment preset="city">` fetches an HDRI from a CDN.** The fetch failed and the exception took the whole Canvas down. Replaced with an explicit light rig (three keys plus two coloured rims) and native `meshPhysicalMaterial` transmission for the splice, so the twin has zero network dependencies. A demo must never require a round-trip to render.
- **2026-09-10 — Dashboard rebuilt as a workstation around the digital twin (supersedes every earlier visual pass).** Reading order is identity bar → twin → inspector → evidence → action. No cards: regions divide by hairlines, sections by spacing. Neutral greys, one accent (blue = selected/interactive), warning/critical only as a word plus a 6px mark; Normal has no colour. Light and dark via `prefers-color-scheme`; Geist + Geist Mono. Tokens and rules live at the top of `dashboard/src/index.css`.
- **2026-09-10 — `dashboard/src/model.ts` is the single map from physical components to real data.** Parts (idler 04, splice, belt, take-up, tail, head, camera) → the fusion subsystems, metrics and sensor positions that actually exist (sensors_sim `default_array`, fusion `evaluate`). Reference lines only where fusion.py cites one. Alerts carry reason *messages*, so events are attributed to parts by the fixed f-string prefixes in `fusion.evaluate()` — change both together. The head pulley is shown as **not instrumented**, not given borrowed numbers.
- **2026-09-10 — Twin geometry is solved, not sketched (`dashboard/src/twin/geometry.ts`).** The belt is a closed loop of exact common tangents and arcs around five pulleys (head drive, tail, gravity take-up with two bend pulleys), so the return strand genuinely drops through the take-up. Carry strand troughs at 35° with transitions at the pulleys. Roll diameter from `BeltModel.idler_diameter_m`; length shortened and labelled so. Self-check: `node --experimental-strip-types src/twin/geometry.check.ts`. Splice rides the loop at the measured speed, phase 0 = passing idler 04 (as `BeltModel` defines it), and is labelled as an estimate.
- **2026-09-10 — Selection model.** One selected part: camera travels there with camera-controls' critically damped `setLookAt` (framing is an exact 8-corner frustum fit, not a bounding sphere), everything else ghosts to 6% (context parts 32%) so an idler under the belt is visible, thin accent `Outlines`, inspector and history strip follow. The splice is followed while it moves. Selection is in the URL hash (`#idler04`) — deep-linkable. Component list keeps a fixed order: rows must not re-sort under the cursor at 2 Hz.
- **2026-09-10 — Colour and identity pass (user: "lacks colour", "no AI-looking palette").** Accent is hammertone machine-tool green (#3f6b57 light / #8fbca5 dark), also the paint on the 3D structure, so UI and machine read as one object; neutrals are cool mill-greys, deliberately not cream. Asset IDs use a DIN-style nameplate face (Barlow Semi Condensed caps). Top bar: the "Line A / CV-204 · since" breadcrumb was called vibe-coded and was replaced by nameplate + labelled "Condition" readout; duration moved into the overview as a sentence.
- **2026-09-10 — Landing screen is the same live 3D scene.** Hero framing (drive end foreground, camera `setFocalOffset` pushes the machine right of the nameplate, slow drift only here), then "Open workstation" / Enter: chrome grows in via native CSS transitions on `height` and `grid-template-columns`, camera flies to the overview. A `#part` deep link skips the landing; the logo returns to it.
- **2026-09-10 — Accessibility.** Native `popover` sheet: theme Auto/Light/Dark, text size 100–150% (CSS `zoom` on panels, not the canvas), high contrast, reduced motion (also honoured from the OS; camera moves become instant), read status aloud and spoken alarm announcements via `speechSynthesis` (announcements use the notifier's deadbanded transitions, never the raw 2 Hz state), push-to-talk voice commands via `SpeechRecognition` (opt-in, panel discloses vendor transcription), keys 1–7/Esc/P/R/V, skip link. Command grammar self-check: `node --experimental-strip-types src/commands.check.ts`. Prefs persist in localStorage `beltguard.prefs`.
- **2026-09-10 — Measured values are not interpolated.** Spring-animated numbers showed values never measured and briefly contradicted their own state label (vision-driven belt_body jumps 83↔11). Readings update in place with tabular figures; motion is kept for camera, panel and disclosure transitions only.
- **2026-09-10 — BUG: drei 10.7 `Outlines` has the `screenspace` branches swapped.** `screenspace` = world units (a 2 m halo); the default is the pixel path. Don't pass the prop.
- **2026-09-10 — BUG: the Google Fonts `@import` was silently dropped for every earlier version** because it followed `@import 'tailwindcss'`, which expands into rules; CSS ignores `@import` after rules. It must come first.
- **2026-09-10 — Headless/background browsers starve the render loop.** With the window hidden, rAF stops: camera travel, ghost fades and the splice only advance when a screenshot forces a paint, and first captures look "broken". Verify end states with the URL hash (first framing is instant) or probe state from JS before concluding a bug.
- **2026-09-10 — KPI delta is honest about its baseline.** Spec asked for "vs last shift". `useBaseline` fetches `/api/health/history?minutes=480` and labels the delta with the span actually covered ("vs 1.5 h ago"); it only says "last shift" once the store holds ~8 h.
- **2026-09-10 — Typography pass + sensor identity.** Section labels became tracked-out uppercase eyebrows (`.heading`), the health-index number went from 28px/500 to 44px/600, readings picked up real weight. Separately, every place that showed a raw wire tag (`vib-idler-04`, `cam-head-01`, ACTIONS' "Section under cam-head-01") now shows the instrument TYPE instead (`model.ts` `SENSOR_TYPE`: Vibration/Thermal/Acoustic sensor, Load cell, Tachometer) — the tag survives only as a hover title. **BUG caught in review:** the first `SENSOR_PART` (physical mount location) was derived from `PARTS[id].metrics`, which conflates "this part BORROWS this reading as fault evidence" with "this sensor is BOLTED here" — it put the load cell and tachometer at the splice, which has no sensor of its own (note field: measured indirectly). Fixed with an explicit map read off `sensors_sim/belt.py` `default_array()` positions instead of derived from PARTS.
- **2026-09-11 — Mobile hero framed, desktop sway bounded to an arc.** Two asks: "redesign the mobile view" and keep the desktop swivel from ever swinging over the text or leaving too much air between it and the machine.
  Mobile: added a plain hairline-bordered box (`lg:hidden`, same breakpoint the workstation grid switches on) with a single mono corner caption, "CV-204" -- purely a caption sitting over the SAME full-bleed scene, not a second render. At phone width the machine used to float on the page with no visual reason to be there; framed, it reads as a pictured asset. Full-width CTA on phone (`w-full sm:w-auto`) for a real touch target.
  Desktop: the idle drift was a continuous, unbounded `controls.rotate()` -- over enough time it visits every angle, including ones where the machine swings toward the text column or, on the far side, sits stranded with a gulf of empty grid between it and the nameplate. Replaced with a bounded sway: `rotateAzimuthTo(base + 6° * sin(t), false)` each frame, `base` fixed at `VIEWS.hero.az` (the angle the framing was actually composed for) and re-armed whenever hero re-frames. Never leaves the composed shot, just breathes around it. Portrait still holds fully still per the prior entry.
  **Could not visually confirm the desktop case.** This Browser pane's real rendering ceiling turned out to be far below what `resize_window` reports succeeding at -- confirmed by forcing a live background colour and watching it clip to the same partial box at both 1400x880 and a moderate 700x460, while `getBoundingClientRect()` lied and reported full size at every level. The pane cannot produce a genuine `aspect > 1.2` (landscape) capture at all; every preset (mobile, tablet, and this pane's own "desktop" reset, measured at ~463x918) is portrait. Confidence here rests on the swivel logic reusing the same `aspect > 1.2` split already proven correct for the portrait framing branch two lines up, plus `camera-controls`' documented `rotateAzimuthTo` -- not on a screenshot. Worth an honest look from an actual wide browser window before calling it done.
- **2026-09-11 — Landing's auto-rotate no longer runs in portrait** (user: "the rotating conveyer setup on mobile... seems pretty weird and out of place"). The slow ambient drift was always meant to read as a deliberate showcase shot on a big desktop hero; at phone scale, with the text sitting right below a SMALL spinning object, it reads as a stray toy instead. Gated the one `controls.rotate()` call in `Rig`'s useFrame on the same `size.width/size.height > 1.2` signal already used a few lines up for the portrait framing branch -- reusing an already-correct portrait test rather than inventing a second one. Dragging still orbits manually either way; only the automatic drift stops.
- **2026-09-11 — Industrial grid + landing restructure, mobile fix (user: "blank unusable space" and "random ass layout" on phone).** `.grid-bg`: a faint 28px blueprint grid (6% of `--fg`, both axes), and even this earns its place -- the twin's hero camera zooms OUT on purpose to leave headroom for the landing text, which used to leave a flat dead backdrop around a small machine; now that space reads as a drawing sheet. Landing went from one `absolute inset-0` box with the text block `my-auto`-centered in the WHOLE viewport, to three explicit flex rows -- header / content (centers only within what's left) / a footer strip pinned to the bottom edge with the sensor-count line moved into it. The `my-auto` version is what actually produced the complained-about dead strip: on a tall phone the text block is short, so centering it in the full height left one lopsided empty band below the button with nothing bounding it. The footer turns unclaimed space into a deliberate edge instead.
  **Investigation note, so the next session doesn't re-chase this:** verifying the fix, `resize_window` to 1400x880 in this Browser pane produced a screenshot where the twin/grid only painted the top-left ~50% of the viewport, black beyond it -- looked exactly like a real rendering regression. `canvas.toDataURL()` pixel sampling at the "black" corners returned `(0,0,0,0)`, which is just the transparent clear colour where there's no geometry (expected -- not evidence of anything). The actual proof came from setting the twin container's background to solid magenta live and re-screenshotting: the magenta filled the same partial box, and `getBoundingClientRect()` up the whole parent chain reported the FULL 1400x880 at every level. Confirmed by resetting to the pane's true native size (measured via `innerWidth`/`innerHeight` after `resize_window:'desktop'`, not assumed) -- 463x918, nowhere near 1400 -- where the same page renders correctly full-bleed with no clipping, and again cleanly at the tablet preset (768x1024). So: this pane cannot actually honour a `resize_window` request past some native ceiling below 1400px wide despite reporting success and despite `innerWidth`/DOM rects claiming otherwise -- it's a tooling ceiling, not an app bug. Don't trust a wide `resize_window` capture here; verify layout bugs at the pane's native size or the mobile/tablet presets, and cross-check anything that looks like a sizing regression with a live-set loud test colour (not just `getBoundingClientRect`, which lied above the ceiling too) before believing it.
- **2026-09-11 — The actual technique from the reference, not another guess at it.** User: "the font like this feels so premium" pointing at the same GraphRAG screenshot, after the Bricolage-only pass still wasn't it. Looked again at what that hero actually does: bold sans word(s) + ONE italic serif accent word, not a single voice. Added Instrument Serif (italic style only) as a fourth, single-purpose voice: `.wordmark-bold` (Bricolage 800) for "Belt", `.wordmark-accent` (Instrument Serif italic, roman weight -- bold would fight its own flourish) for "Guard", sized up 1.08em since the serif's thin strokes read smaller than the grotesk at equal px. Reserved for the landing hero only, same as every earlier display treatment -- the small header wordmark and a selected part's name stay plain Bricolage bold, one accent word would be illegible at 14px anyway.
- **2026-09-11 — Controlled glass, on request against a specific reference (a different repo's Streamlit "GraphRAG Adversarial Defense Console" screenshot).** Flagged first: that reference's actual CSS (`backdrop-filter: blur(22px)`, radial-gradient glow behind the hero, `box-shadow: 0 0 18px` glowing dots, a shimmer-sweep animation) is close to a checklist of what this app's own from-scratch rebuild was explicitly told to remove. Given three options, the user picked the middle one: real frosted glass, no glow/gradient/shimmer. `.glass` in index.css — `color-mix(in srgb, var(--surface) 78%, transparent)` fill (dense enough that AA contrast doesn't depend on what's blurred behind it), `blur(20px) saturate(130%)`, a hairline border tinted 16% toward the accent. Spent only where something genuinely floats over other content, which is what makes it read as glass rather than a translucency filter with nothing to bend: the accessibility/phone popovers, the diagnostics `<aside>` (floats beside the live twin), and the twin's own floating part-callouts and Reset-view button (float directly over the rendered 3D scene, so the blur has real texture to work with). Not applied to anything in normal document flow with nothing behind it — that's just a hazy background for no reason.
- **2026-09-10 — Wordmark tried Grand Hotel (script) + Fraunces (soft serif) — user called it back: "the fonts... arent complementing," wanted "Inter semi bold 600 or smth."** Superseded same day, twice. First pass went all-Geist (600, -0.04em tracking) per that ask; called back again ("make it A1"). **Landed on three type voices, not one or two** (`--font-display`, index.css Type note): Bricolage Grotesque 700/800 spent ONLY on the wordmark and a selected part's name (`Inspector.tsx` PartView `<h2>`) — the two moments allowed to look like a brand, not an instrument — Geist for everything else, Geist Mono for every reading (non-negotiable: a twice-a-second number needs true tabular lining or it jitters). Bricolage was picked over yet another Inter-alike specifically because Geist/Inter-everywhere is what makes a UI read as templated in the first place; its own opsz axis (12..96) is registered so it stays sharp at display size rather than just scaling up a text cut. Barlow Semi Condensed and the DIN-nameplate `.plate` treatment from the attempt before that are both gone.
- **2026-09-10 — Google sign-in, client-side only (`src/auth.ts`), gates entry only when `VITE_GOOGLE_CLIENT_ID` is set** (`dashboard/.env`, see `.env.example` for how to get one) — unconfigured, landing behaves exactly as before. Google Identity Services script + `google.accounts.id.renderButton` (their own button, not a lookalike — required by their branding terms and more reliable). The ID token's payload is decoded for name/photo **without verifying its signature** — fine for personalising a demo, explicitly NOT access control; a deployment gating real data needs the backend to verify against Google's public keys and mint its own session. Self-check for the decode path: `node --experimental-strip-types src/auth.check.ts`.
- **2026-09-10 — "Open on phone" QR (`components/PhoneSheet.tsx`).** Encodes `http://<LAN-ip>:<port><path><hash>` so scanning opens the exact same view, live, on a phone on the same Wi-Fi — the phone gets its own WebSocket to the same backend, so it is not a synced session (pause/selection are per-device), and it does its own Google sign-in if that's configured (no cross-device session handoff, out of scope). The browser has no API to ask the OS for its own LAN address, so `vite.config.ts` grew a dev-only middleware (`/__lan-ip`, Node's `os.networkInterfaces()`) plus `server.host = true` so the dev server actually binds `0.0.0.0` and is LAN-reachable at all. Both are dev-server-only; a production build shows the manual-URL fallback instead of a broken QR. Uses the `qrcode` package (added as a dependency — real QR encoding is not a few-lines job).
- **2026-09-10 — Mobile pass.** TopBar and the corner Accessibility/phone buttons collapse to icon-only under `sm`/`xl`; the `UserChip` menu is a real toggle button (not a hover card — hover is unreachable on the touch phone this whole feature is for). **BUG:** the landing hero's machine and wordmark overlapped badly in portrait — the existing `aspect <= 1.2` branch pushed the camera's focal offset down by only `halfH * 0.35`, nowhere near enough once the script wordmark's height grew; fixed by widening portrait's `pad` (zooms out further) and the offset to `0.62`.
- **2026-09-10 — BUG: `<Canvas shadows>` (the boolean shorthand) asks for `PCFSoftShadowMap`, which this three.js version warns is deprecated, on the first shadow render.** Switched to `shadows="basic"` (`BasicShadowMap` — this scene's contact shadow doesn't need soft edges). Confirmed via a `console.warn` patch that it doesn't recur; the many stale copies seen while iterating were the Browser pane's console buffer surviving across `navigate()` reloads, not a live per-frame warning — do not trust an accumulated count without checking whether new ones are still arriving.
- **2026-09-11 — Desktop hero clipped off the right edge on a real laptop** (user: "the conveyer in laptop mode is going a bit out in right side we hv plenty space in left"). The landscape focal offset (`Twin.tsx`, hero framing) was `-halfH * aspect * 0.3` — unbounded, so it grew with aspect ratio with no ceiling; on an ordinary 16:9 laptop that was already enough to push the machine's near edge past the frame while the left column, which the offset exists to clear FOR, used barely a third of its width. Capped it: `-min(halfH * aspect * 0.15, halfH * 0.45)`. Halved the base factor (0.3 -> 0.15) so a typical laptop no longer clips, and added the `0.45`-of-`halfH` ceiling so a 21:9 ultrawide can't reinvent the same bug at a different aspect. Bumped `hero.pad` 1.12 -> 1.25 to give the smaller offset a bit more room to work with.
  **Not visually verified.** Same pane ceiling as the swivel entry above — this Browser pane cannot render a genuine landscape/wide capture, so the fix is built directly from the user's own on-device report, not confirmed by a screenshot here. Worth a real check from an actual wide window.
- **2026-09-11 — The CV-204 bordered frame box (previous entry) was itself the "trash" the user meant** (user, next message: "the mobile frontend is trash like the landing page only"). A hairline box drawn around mostly-empty space reads as a broken placeholder, not an intentional caption — the exact mistake called out and reverted two sessions ago for the SAME reason on a different element (see the "hairline box... read worse than the empty space" note further up). Removed it outright: `Landing.tsx` phone layout is now a plain fixed-height spacer (keeps the wordmark clear of where the portrait camera parks the machine) followed by content that lives in the flow, no border, no caption.
  **Follow-up same session:** the content block still centered only `sm:` and up, top-anchoring (`justify-start`) below the spacer on phone. On an actual tall screen that dumped ALL the leftover vertical space into one dead strip between the sign-in button and the footer — better than a fake frame, but still visibly empty. Dropped the `justify-start`/`sm:justify-center` split for plain `justify-center` at every width, so the slack splits above and below the text block instead of pooling in one place. Screenshotted at the mobile preset (375x812) to confirm — this one IS a portrait capture, within the pane's actual rendering ceiling, so the screenshot is trustworthy here.
- **2026-09-11 — Real cross-device session hand-off for "Open on phone"** (user: "dont keep any login in mobile like if i scan the same session runs" — a correction to the 2026-09-10 PhoneSheet entry, which explicitly scoped cross-device handoff OUT). Added an opaque, single-use, 120s-TTL pairing token: `POST /api/pair` (`backend/app.py`) takes the already-decoded Google profile the client already trusts (see auth.ts's own signature-not-verified caveat) and hands back a random token; `GET /api/pair/{token}` pops and returns the profile once, then 404s forever after — expired or already-used tokens are indistinguishable, both are just "gone." The QR code (`PhoneSheet.tsx`) embeds only that token in its URL (`?pair=...`), never the profile — the system-level rule against personal data in URLs is satisfied by construction, not by care. `auth.ts` redeems it in a `useEffect` that runs unconditionally on load (a `?pair=` param has no other reason to exist), then strips it from the URL via `history.replaceState` so it can't be bookmarked, shared, or re-redeemed by reloading. Verified two ways: `backend/test_pair.py` (round trip, single-use, expiry, run via `.venv/Scripts/python.exe -m backend.test_pair`) and live in the Browser pane — set a fake signed-in profile via `localStorage` on one tab, opened the QR sheet, confirmed the URL carried a token and the copy read "already signed in," then loaded that exact URL in a second tab with no prior localStorage and confirmed the profile was adopted with no sign-in gate and the `?pair=` param was gone from the address bar.
  **Trust boundary, stated plainly so it isn't rediscovered as a surprise:** the profile handed to the second device carries exactly as much trust as the client-side JWT decode already has — i.e., none, cryptographically. This feature moves an unverified identity to a second screen faster; it does not add or remove verification. A deployment that needs real access control still needs the backend-verifies-Google's-public-keys work called out in auth.ts, independent of this.
  **Environment note:** `backend/test_pair.py`'s `TestClient` needs `httpx2` (Starlette's test client dependency as of the `starlette==1.6.0` now installed) — not listed anywhere since this repo has never tracked test-only deps in `requirements.txt`. Installed ad hoc (`pip install httpx2`) when the self-check failed with `RuntimeError: ... requires the httpx2 package`; not added to `requirements.txt` since nothing else in this repo's test setup is tracked there either.
- **2026-09-11 — Made the backend Railway-deployable and the frontend Vercel-deployable** (user: "make it vercel/railway deployable compatible"). Two real blockers, both fixed with config, not restructuring:
  1. The root `requirements.txt` also pins the vision *training* stack (torch `+cu128`, onnxruntime-gpu, ultralytics, roboflow, ...) — `torch==2.11.0+cu128` isn't resolvable from plain PyPI at all (that build only exists on PyTorch's own index), so a host doing `pip install -r requirements.txt` would fail outright before the backend ever started. Added `backend/requirements.txt`, the actual runtime set for `app.py` + `infra/broker.py` + `sensors_sim/run.py` (fastapi, uvicorn, websockets, paho-mqtt, amqtt, numpy, python-dotenv — traced from real imports, not guessed), and `railway.json` at the repo root pointing the build at that file and the start command at `uvicorn backend.app:app --host 0.0.0.0 --port $PORT`. **Verified, not just written:** installed `backend/requirements.txt` into a throwaway venv (`python -m venv`, no access to anything else in this project's real venv) and booted the backend from it on a spare port — broker connected, sensors live, confirming the slim file is actually sufficient and the deploy path works end to end.
  2. The backend hardcoded `localhost`/`1883` for MQTT and `http://localhost:3000` for CORS — fine when everything is one machine, wrong the moment the broker and the API are different Railway services, or the frontend is a different origin (Vercel) entirely. Both now read `MQTT_HOST`/`MQTT_PORT`/`CORS_ORIGINS` env vars (same defaults, so local dev is unaffected) — verified by booting the isolated-venv backend a second time with `CORS_ORIGINS=https://example.vercel.app,... MQTT_HOST=127.0.0.1` and confirming both the broker connection and the `Access-Control-Allow-Origin` response header actually reflected the overrides.
  On the frontend side, every `fetch('/api/...')`, the `/ws` WebSocket URL, and the `/stream` camera `<img>` were relative paths that only resolve correctly because `vite.config.ts` proxies them to `localhost:8010` in dev -- a static Vercel build has no such proxy, so they'd 404 against Vercel's own origin. Added `dashboard/src/config.ts` (`API_BASE` from `VITE_API_BASE`, empty by default so behavior is unchanged unless it's set; `WS_URL` derived from it) and pointed all four call sites at it. **BUG caught by the self-checks, not by inspection:** `config.ts`'s `import.meta.env?.…` and its `location.host` fallback both blew up under plain Node the moment `auth.check.ts` transitively imported it (`auth.ts` -> `config.ts`) — `import.meta.env` and `location` simply don't exist outside Vite/a browser. Guarded both the same way `auth.ts` already guards its own `VITE_GOOGLE_CLIENT_ID` read (`?.`, and a `typeof location === 'undefined'` check) — the self-check is what surfaced this before it shipped, not a manual review.
  **Not done, deliberately:** no `vercel.json` — this is a plain Vite SPA with no client-side routing, so Vercel's own "Root Directory: dashboard" + auto-detected Vite preset is sufficient; a rewrites config would be solving a routing problem this app doesn't have. No automated multi-service Railway setup for the broker/simulator either — those stay optional manual services per README §8.1, since a deployed backend with no broker already degrades to the same honest "no data yet" state the console shows locally before `sensors_sim` starts, which is a legitimate demo state, not a bug to route around.
- **2026-09-11 — Mobile hero rated "4/10... the 3D model isn't even visible and too much empty space"** — the portrait zoom-out from the "mobile hero framed" entry above (`pad * 1.8`) turned out to shrink the machine into something easy to miss, in a reserved band (`h-[22vh]`) with a lot of grid around it doing nothing. Two independent fixes, not one bigger one:
  1. `Twin.tsx` portrait framing: `pad * 1.8` -> `pad * 1.3` (noticeably bigger machine) and the downward focal offset `halfH * 0.62` -> `halfH * 0.5` (needed less push once the fitted box is bigger). `Landing.tsx`'s reserved band grew to match, `22vh/190px` -> `32vh/270px`, sized together with the camera change rather than independently -- a bigger machine in the same small band would just clip.
  2. The OTHER empty-space complaint was the gap between the CTA and the footer, from the "splits above and below" centering fix two entries up -- true on the numbers (the gap didn't grow), but centering a short block in a large flex-1 region doesn't remove slack, it just relocates it, and there was still plenty to relocate. Root cause fix, not another reshuffle: on phone, dropped `flex-1`/`justify-center` off the content block entirely (kept for desktop via `lg:flex-1 lg:justify-center`, where the leftover really is modest). Content now sits at its own natural height and the footer follows directly after -- true leftover becomes trailing space below the footer, at the actual end of the page, which reads as "short page" rather than "something's missing". **Verified this doesn't reopen the 150%-text-size accessibility case** the old `flex-1` + `overflow-y-auto` was quietly also protecting: `min-h-0` on that same div still lets it flex-*shrink* below its content height even without flex-*grow*, so when a real overflow happens (tested by setting `beltguard.prefs.scale` to 1.5 directly), the div compresses and its own scrollbar appears, and the sign-in button is still reachable by scrolling it. Confirmed by screenshot at each step (375x812, a portrait capture this pane renders correctly) rather than by reasoning about the flexbox alone.
- **2026-09-11 — "The swivel isn't working in laptop" -- checked for an actual bug rather than assuming the report was wrong.** The Browser pane still can't paint a real landscape capture, but a screenshot isn't the only way to check a camera is moving: read the live camera/controls state instead. Added a temporary `window.__dbg` handle to the framing effect exposing `camera` and the `CameraControls` instance, forced the pane to a 1400x800 viewport (visually broken here, but the canvas's actual `size.width/size.height` -- what the framing math reads -- genuinely resizes even though painting doesn't), and polled `azimuthAngle` and `camera.position` a few seconds apart. **Both changed** (azimuth 0.892 -> 1.054 rad, well within the composed ±6° band around `az: 0.95`; position moved correspondingly) -- the swivel mechanism is not broken. Removed the debug hook immediately after confirming (never shipped).
  So the code was correct but the complaint was still real: 6 degrees over a 17 s period is roughly 4 minutes of arc per second -- genuinely there, but easy to mistake for "not doing anything" on a quick glance, and this app's `reduced` flag (`prefs.ts`) also honors the OS `prefers-reduced-motion` setting unconditionally, which a laptop can have on without the user thinking of it as an app setting -- either explanation, or both, could produce "isn't working" from a real user even with working code. Fixed the part actually worth fixing regardless of cause: widened and sped up the sway (6° / 17 s -> 10° / 11 s) so it reads as motion at a glance without becoming the old unbounded spin this replaced. If it's still not visible on the reporting laptop, the next thing to check there specifically is the OS animation/reduce-motion setting, not this code.
- **2026-09-11 — "Make final enhancements... top tier... clean codebase" -- audited before touching anything, rather than sprinkling changes across backend/frontend/UI/animations/glass on request alone.** `npx oxlint` was already fully clean of anything real: every warning (`react(refs)` on `auth.renderButton`/`auth.user.*` in Landing.tsx, `react(immutability)` on `tex.offset.x`/`geo.attributes.position` in Twin.tsx, `react(only-export-components)` in ui.tsx/Charts.tsx) is the generic React linter not understanding React-Three-Fiber's imperative-mutation model, where writing to a ref's `.current` or a geometry's buffer inside `useFrame` every frame IS the correct, required pattern -- "fixing" the Twin.tsx ones would have broken the belt-texture scroll and the travelling splice. No console.log/debugger left in the frontend, no stray print()/pdb in the backend outside `notify.py`'s documented "no SMTP configured" logging and the test_*.py self-checks' own expected output. So the codebase itself wasn't dirty; two real, narrow things were:
  1. **The production bundle was one 1.76 MB chunk** (`vite build`'s own oft-ignored size warning, sitting unaddressed since the first build). Nothing above the fold needs three.js/@react-three/fiber/drei/camera-controls synchronously -- the header, wordmark and CTA can paint before any of it loads. `App.tsx`'s `Twin` import became `lazy(() => import('./twin/Twin')...)` behind a `<Suspense>` whose fallback (`grid-bg` + `bg-[var(--viewport)]`) matches Twin's own outer wrapper exactly, so the heavy chunk loading in behind it is invisible -- same backdrop before and after, no flash. Verified by rebuilding: 1,761 KB / 503 KB gzip -> 749 KB / 229 KB gzip main chunk, plus a 1,012 KB `Twin` chunk that now loads separately and lazily. This is the one thing keeping "open on phone" light over a real Wi-Fi link rather than a dev-machine loopback, where the difference was never visible before.
  2. **The backend's `/` 404'd** -- no liveness route at all, which is exactly the path Railway's default health check hits (see the deployment work two entries up) and exactly what a person opening the bare backend URL would try first. Added a two-line `GET /` returning `{service, status, docs: "/docs"}` (FastAPI's own Swagger UI, always free, was already there and just never pointed to). Also caught in the same pass: `/api/pair` and `/api/pair/{token}` were never added to the README's endpoint table when the pairing feature shipped -- docs said less than the API actually does. Fixed both.
  **Deliberately not touched:** glassmorphism, animation style, and overall visual language. Nothing concrete was reported broken there since the last redesign passes, and this app's `.glass` usage is the result of an explicit negotiation (see the "Controlled glass, on request against a specific reference" entry) balancing against the project's own from-scratch anti-glow rules -- widening that scope on a vague "and all" would risk re-introducing exactly what those rules exist to keep out, not "top tier" it further.
- **2026-09-11 — User pushed back on the entry above: "glassmorphism and glow are separate things, glass looks very premium when the cards are interactive as well."** Correct, and worth taking at face value rather than re-litigating: the project's actual anti-glow rule (see "Controlled glass" entry) was always specifically against `box-shadow` bloom, radial gradients and shimmer sweeps, never against translucency + blur itself, which is what `.glass` already is. The previous entry's caution was about scope creep on a vague "and all," not a reason to refuse a concrete, correctly-scoped ask -- this one names an actual target (interactive cards) and a real distinction, so acted on it.
  Added `.glass-card` (`index.css`), the interactive counterpart to `.glass` for surfaces that live INSIDE an already-glass panel: same translucent-fill-plus-tinted-border language, but deliberately no `backdrop-filter` of its own -- blurring content that already sits behind one blur layer costs a repaint for no visible difference. What makes it read as glass is the fill and the border, and what makes it read as premium *interactive* glass is that both shift toward the accent colour on hover/press (`color-mix` against `--hover`/`--press`/`--accent`) -- never a shadow, never a glow. Applied it to the "Components" list in `Inspector.tsx`, converting seven flat `.row` list items into a 2-column grid of these cards -- the clearest existing "list of things you click" in the app, and the one most likely to read as dated next to the rest of the redesign.
  **Verification note for the next session:** this pane's screenshot capture went stale mid-verification -- `document.getElementById('diagnostics')` and other DOM queries confirmed the workstation view had genuinely entered and the cards existed with all 7 parts, but repeated screenshots kept painting the old landing view regardless of wait time. Rather than trust a broken screenshot OR give up on verifying, fell back to what actually answers "does this work": read the compiled production CSS directly (`grep` on `dist/assets/*.css` after a fresh `vite build`) and confirmed every `.glass-card`/`:hover`/`:active` rule survived with the exact values written, then confirmed via `getComputedStyle` in the live page that a real card element resolves those exact colours, then `.click()`'d a card programmatically and confirmed `location.hash` and the heading actually changed to that part's detail view. Three independent layers (compiled output, applied computed style, functional click-through) all agreed; the one thing NOT confirmed this session is the pixel-level "does it look good," which needs a real screenshot from a working pane or the user's own eyes.
- **2026-09-11 — User, immediately after seeing it: "the glassmorphism isnt proper its like normal cards only."** They were right, and the *reasoning* behind `.glass-card` two entries up was the actual bug, not just its numbers. Two things were wrong, found by measuring rather than re-guessing:
  1. **No backdrop-filter at all**, on the theory that blurring content already behind one blur layer does nothing. True in a literal sense, wrong in effect: blur is the single property that most reads as "glass" to a person looking at it, and without it translucency alone is just a see-through box. Added `backdrop-filter: blur(14px) saturate(160%)` back.
  2. **The fill was built from `--surface`** (`#171a18` dark / `#f8f9f8` light), which sits almost exactly on top of the panel it's nested in -- so at any believable opacity the card was nearly indistinguishable from its own background. `--hover`/`--press` already solve this exact problem elsewhere in this file (a white wash in dark mode, an ink wash in light mode -- whichever direction actually lifts off that theme), so rebuilt the fill on `--fg` directly (same lift direction, parameterised to a stronger 7% since a resting "material" needs more presence than a fleeting hover state at 5%).
  **A second, unrelated bug turned up while fixing the first:** the inset highlight (`box-shadow: inset 0 1px 0 color-mix(in srgb, var(--hover) 200%, transparent)`) was rendering as `none`. Cause: `color-mix()` stops are 0-100%; `200%` is out of range, so the whole declaration was invalid and the browser dropped it silently -- confirmed via `getComputedStyle().boxShadow`, not assumed from reading the CSS. Rebuilt off `--fg` at a valid 14% instead. Two lessons worth keeping: a "that theoretically shouldn't matter" simplification is exactly the kind of thing to cut when a person says the result looks wrong, and an invalid CSS value doesn't throw or warn in the console -- it just silently no-ops, so a property that "should be there" is worth confirming with `getComputedStyle` rather than trusting the source.
  Verified this round with an actual screenshot (the pane recovered after closing and reopening the tab) -- the cards now visibly lift off the panel with a soft frosted sheen, not just in computed-style numbers.
- **2026-09-11 — "the 3d model aint swiveling in laptop and not rotating in mobile."** Checked the swivel mechanism itself first, the same way as the earlier "isn't working" report (live `azimuthAngle`/`camera.position` polling): still genuinely oscillating, so that specific mechanism was never the bug this time. But "not rotating" turned out to mean something more basic than the idle sway -- **manual orbit-drag didn't work at all**, on the landing screen, on any input device, and that's a real, universal bug that happened to look like two different platform-specific complaints.
  Root cause, found by checking `document.elementFromPoint()` at the drag coordinates instead of re-guessing: it resolved to `<div class="absolute inset-0">` -- App.tsx's `AnimatePresence` wrapper around `<Landing>` (`motion.div key="landing"`) -- not the canvas underneath. `Landing`'s OWN root div has always carried `pointer-events-none` precisely so clicks pass through to the 3D scene except on its own opted-in children (buttons etc.) -- but the wrapper App.tsx puts AROUND `Landing`, added later so `AnimatePresence` has something to animate on exit, never got that same treatment. An unstyled `absolute inset-0` div is a full-viewport, perfectly ordinary hit-test target by default, so it silently ate every click, hover and drag meant for the twin underneath, for as long as the landing screen was showing. Added `pointer-events-none` to that wrapper too. Verified by re-running the exact failing drag afterward: `elementFromPoint` now resolves to the canvas itself, and a drag moves `azimuthAngle` by roughly a full radian instead of not at all -- tested on both a landscape (1400x800) and a mobile (375x812) viewport.
  **A dead end worth recording so it isn't re-walked:** before finding the real bug, spent time on a real-but-irrelevant issue -- the canvas's own `touch-action` computed style was `auto`, not the `none` that `camera-controls` sets internally on connect (traced into the library source: `_addAllEventListeners` does set it, but React-Three-Fiber's own resize-driven style updates on the canvas element appear to overwrite that inline mutation later). Added `style={{ touchAction: 'none' }}` to `<Canvas>` in `Twin.tsx` to make it survive (R3F applies `style` to the wrapper div it renders, and per spec `touch-action: none` on an ancestor still constrains a descendant regardless of the descendant's own declared value). That fix is real and worth keeping -- a touch drag is genuinely ambiguous between "orbit the camera" and "scroll the page" without it -- but it was NOT the reported bug: dispatching synthetic events at the drag coordinates showed **zero** events reaching the canvas at all (not `pointerdown`, not `touchstart`, nothing), which only made sense once the pointer-events-blocking wrapper was found. A `touch-action` gate would still have let the initial event through; a blocking ancestor div does not.
- **2026-09-11 — "what even is idler?"** A fair question this UI wasn't answering. `PARTS[id].kind` (`model.ts`) is deliberately technical ("Carry idler set, three-roll") -- correct for an operator, meaningless to anyone else, including a judge who has never stood next to a conveyor. Added a required `about` field to every part: one plain sentence, no jargon, saying what the thing actually IS or DOES rather than classifying it (e.g. idler04: "A set of rollers that the belt rides on, holding its shape and keeping it running straight"; head pulley: "The motor-driven roller that actually pulls the belt around the loop -- the conveyor's engine"). Surfaced in two places: as the `title` (hover tooltip) on each Components grid card, and as a permanent line under the kind/location caption in the part detail view (`PartView`, Inspector.tsx) -- the tooltip is a bonus for a mouse, the detail-view line is the one that actually answers the question on any device.
- **2026-09-11 — "the glassmorphic part could be much better."** Extended the same techniques from the two entries above rather than inventing new ones. `.glass` gained a plain elevation shadow and a 1px inset top highlight -- neither is a glow (no colour, no blur-behind-the-element bloom): a glow tints the air around an element, an elevation shadow is just what any object sitions above another casts, and a highlight line is one flat pixel, not a gradient. Split that pair into a new `.glass-panel` modifier rather than folding it into `.glass` itself, because `.glass` is also used on the twin's small floating callout labels (`Twin.tsx`), which already carry their own fitted `shadow-[0_1px_3px_...]` sized for a few characters of pill -- and since `.glass` lives outside any `@layer`, it would have beaten that Tailwind utility on the cascade and stamped a panel-sized shadow onto every tiny label. `.glass-panel` applied only to the diagnostics aside and the user menu dropdown; the two `.sheet[popover]` surfaces already had their own elevation shadow, so just gained the matching inset highlight added to their existing `box-shadow` list. `.glass-card` (previous entry) pushed further too: fill 7% -> 9%, blur 14px -> 18px, inset highlight 14% -> 16% -- all the same recipe, just turned up now that the recipe itself is known to be correct.
- **2026-09-12 — "swivle and rotate still not working" -- one more blocking layer, found the same way as the last one.** The `AnimatePresence` wrapper fix (previous entry) was real but incomplete. `elementFromPoint` at coordinates well clear of the visible text -- e.g. (700, 400) on a 1400-wide viewport, nowhere near the ~34rem text column -- was STILL resolving to a `<div>`, not the canvas: specifically `Landing.tsx`'s content wrapper (`pointer-events-auto flex ... lg:flex-1 lg:justify-center`). Root cause: a flex-column child stretches to the full width of its parent by default (`align-items: stretch`), so even though the visible text inside caps at `max-w-[34rem]`, this wrapper's own -- and it's the one carrying `pointer-events-auto` -- hitbox was the FULL VIEWPORT WIDTH on desktop, not just the width of the text sitting inside it. It was silently eating clicks and drags across nearly the whole screen, `lg:flex-1`'s vertical stretch made it tall too, and this had nothing to do with the AnimatePresence wrapper fixed last time -- a second, independent hole in the same net. Fixed with `self-start` (shrinks the box to its content's width instead of stretching), left off the footer deliberately since its own `border-t` visually depends on spanning full width and it's a thin strip at the very bottom, not central "drag to orbit" real estate the way the content block was. Re-verified with the same method as before -- `elementFromPoint` at several points across the width, then an actual drag with an azimuth reading before and after -- on both viewport sizes, this time genuinely passing all the way through to the canvas.
  **Second lesson from this same session, worth internalizing rather than re-learning:** spent real time chasing what looked like the onboarding hint (below) dismissing itself instantly on every reload, timestamped every call to figure out why -- and the timestamps (`t=6171`, `t=6242`) showed the 6-second timer firing exactly on schedule. The "bug" was that checking the result in a SEPARATE tool call after the action, with normal reasoning and round-trip time in between, routinely burns several real seconds -- enough to clear a 6-8 second window without anything being wrong. When verifying a timer or animation programmatically, either read a live timestamp (`performance.now()`) inside the same check rather than assuming your own elapsed wall-clock time is near-zero, or keep the whole test (start the timer, take the measurement) inside as few tool round-trips as possible.
- **2026-09-12 — Onboarding hint: "drag to rotate," shown once, ever.** Added alongside the drag fixes above, since a manual-orbit affordance that has never actually worked is also one nobody has ever had a reason to notice existed. Dismissed by the first real drag (reuses the existing `moved` signal `Rig` already tracks) or an 8s timeout, whichever comes first; the "seen" flag persists in `localStorage` so it genuinely never shows twice. Positioned under the header (`top-16`) rather than above the footer -- an earlier attempt at `bottom-16` assumed Twin and Landing share exactly one coordinate space (true) and that the footer sits at a predictable enough offset from the bottom in both portrait and landscape to safely clear (turned out not reliably true); anchoring from the header side, close to where the machine actually sits in both layouts, sidesteps needing to know exactly where the footer is at all. `pointer-events-none` on the hint itself was non-negotiable given what this whole feature exists downstream of.
- **2026-09-12 — "auto rotate... like it earlier was"** confirmed still working via the same live `azimuthAngle` instrumentation as before (still oscillating correctly on a fresh load) -- nothing regressed it; the drag-blocking bugs above were a separate, additional problem from the swivel itself, not a cause of it. If it's still not visible on a real device after this session's fixes, the two things actually worth checking there are the OS reduce-motion setting (still the one variable this environment can't observe) and whether the page being tested has actually picked up these commits.
- **2026-09-12 — Moved deployment off Railway/Vercel onto Render for both services** (user: "we are doing it on render make it render compatible"). No restructuring needed -- the split from the 2026-09-11 deploy work (slim `backend/requirements.txt`, env-configurable `CORS_ORIGINS`/`MQTT_HOST`/`MQTT_PORT`, `VITE_API_BASE` in `dashboard/src/config.ts`) is host-agnostic; only the host-specific config file and docs changed. Replaced `railway.json` with `render.yaml` at the repo root, a Blueprint defining both services in one file: `beltguard-backend` (type `web`, same build/start commands as before) and `beltguard-dashboard` (type `static`, root `dashboard/`, `npm run build` -> `dist/`) -- one `render.yaml` covers what previously needed two separate platform dashboards. Cross-wired `CORS_ORIGINS` and `VITE_API_BASE` to each other's expected `https://<name>.onrender.com` URL rather than leaving them blank, with a comment flagging that a taken name changes the real URL and both values need a manual update in that case (Render Blueprints have no built-in "full URL of sibling service" interpolation, only bare hostname). Updated every remaining Railway/Vercel mention (README §8.1, `backend/app.py` x2, `backend/requirements.txt`, `dashboard/.env.example`, `dashboard/src/config.ts`) to match. **Deliberately not done:** no `render.yaml` service for the MQTT broker or sensor simulator -- both stay optional per README §8.1 (unchanged reasoning from the Railway entry), and if added later, the broker specifically needs Render's Private Service type, not Background Worker, since a worker has no inbound port and the broker must accept connections from the backend.
- **2026-09-12 — Local environment repairs, unrelated to app code but worth logging so the next session doesn't re-diagnose them.** (1) "health index seems constant" was `sensors_sim.run` having finished its fixed-length scenario and exited -- the backend just re-broadcasts the last known reading forever with no ingest, which looks exactly like a frozen UI but is a demo-data lifecycle fact, not a bug; restarting the simulator is the whole fix, and this will recur every ~9 minutes at `--speed 5` for anyone running a long local session. (2) "camera feed not working" was `vision.service` needing `ultralytics`, never installed in this venv. Installing it (unpinned) pulled a plain PyPI `torch` 2.14.0 that upgraded over the working `2.11.0+cu128`, and the new build's CUDA DLLs (`c10_cuda.dll`) fail to load on this machine -- broke `import torch` outright, worse than before. Attempted the standard fix (`pip install --force-reinstall torch torchvision --index-url .../whl/cpu`), which pip reported as successful (`torch-2.14.0+cpu` installed), but `c10_cuda.dll` and other CUDA libraries were STILL present in `torch/lib/` afterward and the same import error persisted -- consistent with the project's known OneDrive-sync-vs-python-venv conflict (see the "heavy artifacts relocated" entries elsewhere in this file), where OneDrive appears to restore files pip has just deleted.
- **2026-09-12 — "camera feed isnt working, take your time, fix properly."** Did the fix the previous entry deferred: moved `.venv` itself outside OneDrive, same remedy already applied to training artifacts, this time applied to the thing that actually kept breaking.
  Two real obstacles on the way there, both discovered by testing rather than assuming: (1) `%LOCALAPPDATA%` inside this tool's own process tree resolves to a Windows per-package virtualized path (`AppData\Local\Packages\Claude_.../LocalCache\...`), not the real `C:\Users\Dell\AppData\Local` -- confirmed by creating a venv there and having Python itself report "Actual environment location may have moved due to redirects, links or junctions" pointing at the virtualized path. This is OS-level package-identity storage redirection, not something `dangerouslyDisableSandbox` touches (verified: disabling it still redirected). A file written there would be invisible to the user's own terminal, making the "fix" worthless past this session. Plain drive-root paths (`C:\beltguard-venv`) aren't subject to this per-profile virtualization and were used instead. (2) The fresh venv's own `pip install -r requirements.txt` pulled in default (CUDA) torch again via `ultralytics`'s own dependency chain, same failure mode as before -- installed the CPU wheels explicitly (`--index-url .../whl/cpu`) and re-pinned `numpy==2.3.5` afterward (the CPU torch install bumped it to 2.5.2, which conflicts with `roboflow`'s own pin -- the exact "second numpy" class of problem the root `requirements.txt` already warns about, just via a different package this time).
  `C:\Users\Dell\OneDrive\Desktop\beltguard\.venv` is now a directory **junction** to `C:\beltguard-venv` (`New-Item -ItemType Junction`), so every existing reference to `.venv/Scripts/python.exe` -- in this file, the README, and anyone's own muscle memory -- keeps working unchanged; only the physical storage moved. The old OneDrive-synced venv was renamed to `.venv.old-onedrive` rather than deleted, kept as a rollback until the new one has proven itself across a real session. Verified end to end, not just at the Python level: restarted broker + backend + sensors_sim + `vision.service` through the (junctioned) `.venv`, confirmed `vision_active: true` and `GET /stream` returning 200, then confirmed in the actual browser that the Inspection-camera part view renders a live annotated frame (tear/patch_repair boxes from the real model) rather than the "Offline" fallback.
- **2026-09-12 — "by swivel... i meant the ranged autorotate," reported not visible for a third time despite two rounds of live-state confirmation.** Rather than re-run the same instrumentation a third time expecting a different answer, switched to a screenshot-diff: two captures of the idle landing hero a few seconds apart, zero interaction in between. The rendered angle WAS visibly different between the two -- so the previous two rounds of "confirmed via azimuthAngle" were not wrong, the motion was just genuinely too subtle to register at a glance, exactly as the very first tuning pass (6°/17s -> 10°/11s) had already tried to fix once and apparently undershot. Rather than reconfirm a third time, widened further: 10°/11s -> **18°/7s**, a visibly larger arc completing in well under half the previous period. Re-verified the same way (screenshot pair, several seconds apart) and the difference is now unmistakable even in this pane's clipped landscape rendering, while staying well inside frame at both extremes -- still a bounded sway, not the spin that was removed for looking broken.
- **2026-09-12 — Overview layout: camera feed promoted out of the Components grid into its own preview tile beside Health index** (user: reposition the camera feed, shift Health index right, "the other 4 cards also seem uniform"). Added `CameraPreview` (`Inspector.tsx`) -- a compact live-MJPEG tile with the same retry-on-drop behaviour as the full `Inspection` component, minus its detection list, clicking through to the camera's part-detail view same as any Components card. Placed it and Health index side by side at the top of Overview (camera left, Health index shifted into the right column), both restyled as `glass-card` for visual consistency with the grid below. Excluding `camera` from the Components grid (it carries no subsystem score, so it was always the one card there that looked structurally different) leaves exactly 6 -- a clean 2x3 grid instead of 7 with an odd one trailing alone in the last row, which is almost certainly what "the other 4 cards" was gesturing at even if the count named didn't land exactly on 6.
  **Caught by a full dev-server restart, not by the code itself:** after this edit, the running dev server's HMR threw `ReferenceError: CameraPreview is not defined` -- looked alarming, but `npx tsc -b` and a full `vite build` both passed clean immediately before, and function declarations hoist regardless of source order in JS, so the ordering itself was never actually wrong. Closing the tab and restarting the dev server outright cleared it with zero errors on load, consistent with React Fast Refresh accumulating bad state over a very long single dev-server session (this one had survived dozens of edits and reloads by this point) rather than anything in this specific change. Worth remembering: an HMR-only error after a long session is itself a signal to rule out with a cold restart before trusting it over a clean build.

## 8. Known Blockers / Open Questions

- **RESULT 2026-09-04 — trained model, and the honest reading of it.** 120 epochs, 57 min. Per-class on the held-out test split:

  | class | P | R | mAP50 | test instances |
  |---|---|---|---|---|
  | belt_joint | 0.944 | 1.000 | 0.995 | **7** |
  | tear | 0.964 | 0.948 | 0.971 | 142 |
  | hole | 0.970 | 0.855 | 0.957 | 113 |
  | impact_damage | 0.986 | 0.968 | 0.980 | 31 |
  | patch_repair | 0.975 | 0.976 | 0.975 | 42 |

  **`belt_joint` recall 1.000 must NOT be quoted as-is.** It means "found all 7 of 7". One miss would have read 0.857; the Wilson 95% CI lower bound on 7/7 is ~0.65, so true recall could plausibly be 65%. The headline class for this PS is the one we can say least about. Do not put 0.995 on a slide without the n=7 next to it.
  **Leakage was checked and is clean:** 0 shared source images between train/valid/test (Roboflow split before augmenting; all 489 augmented variants stay inside train). So the other classes' scores are trustworthy.
  Splits hold belt_joint 60/14/7 (train/valid/test) — the class is scarce everywhere, not just at test time.
- **`Belt Joint` has only 41 labelled instances** across all datasets found. This is the single most important class for PS 26008 and it is the rarest. Mitigation plan: heavy augmentation, class-weighted loss, and honest reporting of per-class recall rather than a flattering overall mAP. May need hand-labelling a top-up set.
- ~~`ui-ux-pro-max-skill` is NOT available.~~ **RESOLVED 2026-09-04** — user installed it from an interactive terminal. `ui-ux-pro-max:*` skills now in the roster (design, design-system, ui-styling, slides, banner-design). Use for the dashboard stage.
- **PyTorch installed is `2.11.0+cpu`** while the machine has an RTX 5060 (Blackwell, sm_120). GPU training needs a CUDA 12.8+ build (~2.5 GB download). Awaiting user go-ahead.
- **Roboflow dataset pages return HTTP 403 to automated fetch.** Exact image counts, class lists and licenses must be confirmed via the Roboflow API (needs a free API key) or a manual browser check before a dataset is committed to.
- ~~Docker Desktop is installed but was not running.~~ **RESOLVED 2026-09-04** — root cause was that **WSL2 is not installed**, which Docker Desktop's Linux engine requires; it launched and died silently. Rather than spend an admin install + reboot on one broker, switched to amqtt. Full round-trip now verified: 260 msgs across all 5 sensor kinds, `simulated` flag intact on the wire.
- ~~The project lives on OneDrive, which is measurably slowing training.~~ **RESOLVED 2026-09-04** — heavy artifacts relocated to `%LOCALAPPDATA%\sih26\` (see decision log). Image read 3.3 -> 38.9 MB/s, epoch time ~80 s -> 23.8 s, full run ~2.5 h -> ~60 min. Ultralytics still prints its slow-access warning (its threshold is aggressive) but the bottleneck is gone.
- ~~The apostrophe in the folder name is a live hazard.~~ **RESOLVED 2026-09-04** — user renamed `SIH'26` -> **`beltguard`**. Verified after the move: venv intact, CUDA still available, and absolute `.pt` paths load correctly, so the `_yolo_path` workaround was deleted as dead code. Full suite re-run green in the new location.
- **onnxruntime CUDA EP does not load** on this machine (ORT 1.29 moved it to a separate plugin package). Irrelevant in practice — ONNX on CPU is already faster than the CUDA `.pt` here, and the Nano will use TensorRT, not onnxruntime.
- **No physical hardware.** Standing constraint, not a blocker for this phase.

## 9. File/Folder Structure

```
beltguard/
├── context.md          # this file — project memory
├── vision/             # defect detection: dataset prep, training, ONNX export, webcam demo
│   ├── paths.py        # resolves heavy-artifact locations (see decision log)
│   ├── models/         # gitignored — final .pt / .onnx weights
│   └── scripts/        # prepare_dataset.py (download+merge) | train.py (train+ONNX export)
├── sensors_sim/        # datasource.py (ABC) | belt.py (physics + 5 sensors) | run.py (scenario->MQTT)
├── backend/            # fusion.py (health scoring) + tests; FastAPI/MQTT-subscriber/SQLite still TO BUILD
├── infra/              # broker.py (amqtt) + test_mqtt_roundtrip.py
├── requirements.txt    # pinned; note the cu128 index URL for torch
├── dashboard/          # Vite+React app — live view, alerts, trends, camera, RUL
│   └── src/            # model.ts (part↔data map) | prefs.ts | voice.ts + commands.ts | twin/ (geometry.ts, Twin.tsx) | components/ (Landing, Inspector, Charts, AccessSheet, ui)
├── scada_sim/          # pymodbus TCP server exposing health state to a mock SCADA
├── predictive/         # cwru.py (real-data validation) | rul.py (trend RUL)
└── docs/               # architecture, hardware-swap guide, real-vs-simulated honesty doc

Outside the repo (NOT synced by OneDrive), created by vision/paths.py:
%LOCALAPPDATA%/sih26/
├── raw/                # Roboflow downloads as-fetched
├── belt_defects/       # merged dataset, unified 5-class taxonomy
├── runs/               # training checkpoints (per-epoch churn)
└── train.log

`.venv/` (repo root) is a directory JUNCTION, not a real folder -- it points
at `C:\beltguard-venv`, outside OneDrive, for the same reason as `sih26/`
above (OneDrive-vs-python-package-install conflicts, this time specifically
CUDA DLLs from torch/onnxruntime getting silently restored after pip
deletes them). `.venv/Scripts/python.exe` and every command in this file
that uses it work completely unchanged -- the junction is transparent to
every existing reference. The pre-junction venv is kept at
`.venv.old-onedrive/` as a rollback, not yet deleted.
**Note for any session running commands through a sandboxed tool (this one
included):** `%LOCALAPPDATA%` may resolve to that tool's own virtualized
per-package storage rather than the real path -- confirmed by Python's own
venv module reporting a mismatch between the requested and actual location.
Plain drive-root paths (`C:\something`, not under any user-profile "known
folder") aren't subject to this and are the safe choice for anything meant
to outlive the current session.
```

## 10. Demo Narrative (for SIH presentation)

- **Current honest status (2026-09-09): feature-complete for the no-hardware phase.** All 11 checklist items in Section 4 are done. The authoritative honesty statement now lives in **`docs/real-vs-simulated.md`** — that is the document to read before pitching, not this bullet.
- One-line version: *the AI models are real and trained on real data; the sensor readings are simulated, and the system says so on screen, on the wire, and in every alert.*
- The three caveats to say out loud before a judge finds them: **belt_joint mAP50 0.995 is n=7**; **CWRU 100% is an easy seeded-defect benchmark**; **the demo timeline is accelerated 672x**.

- What is genuinely working live vs. simulated must always be stated honestly here, so the pitch never overclaims.
- **Superseded status (2026-09-04):** Repo scaffolded on its own git repo. Sensor simulation layer **built and passing 7 physics self-checks** — vibration/temperature/load/speed/acoustic driven by one shared belt model, every reading structurally flagged `simulated=True`. Vision datasets verified via API and licensing confirmed (CC BY 4.0). **Not yet running:** no model trained, no MQTT bus, no backend, no dashboard. Nothing is being presented as real hardware data.

---

**Reminder to Claude:** Before ending any working session, update sections 4, 5, 6, 7, 8, and 9 with whatever changed. This file should always let a fresh session pick up exactly where the last one left off with zero re-explaining needed from the user.
