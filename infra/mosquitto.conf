# Demo broker. Anonymous access is fine on a laptop-local bind; a site
# deployment would use per-device credentials and TLS on 8883.
listener 1883
allow_anonymous true
persistence false
log_dest stdout
